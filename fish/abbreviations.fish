# general
abbr -a mk mkdir -p
abbr -a rr rm -r

# fish
abbr -a reload source ~/.config/fish/config.fish
abbr -a config $EDITOR ~/.config/fish

# network
abbr -a hosts sudo $EDITOR /etc/hosts

# custom
abbr -a pls please
